export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Next.js + Express + MongoDB (Monorepo)</h1>
      <p>Go to <a href="/users">/users</a> to fetch users from the backend.</p>
    </main>
  );
}
